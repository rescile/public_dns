# The resident is a top level node that represents the owner of a subscription in the graph
# Specify the source type
origin_resource = "subscription"

# Add external input for subscription data
tenants = { "json!" = "resident.json" }

# Command line paramter
resident = "{{- tenant | default(value='rescile') -}}"

# Create resource type
[[create_resource]]
resource_type = "resident"
relation_type = "DEFINED_BY"
name = "{{- tenants[resident].name | lower -}}"
[create_resource.properties]
commercial_admin = "{{- tenants[resident].commercial_contact -}}"
technical_admin = "{{- tenants[resident].technical_contact -}}"
description = "The resident {{- tenants[resident].name | capitalize -}} is an organization that has private, secure access to the dedicated set of computing resources and services within the cloud infrastructure. Essentially, it is the technical term for the customer's dedicated partition where their data, applications, and virtual machines reside, isolated from other cloud customers."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"
